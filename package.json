{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "dev:api": "pnpm --filter ./artifacts/api-server run dev",
    "dev:web": "pnpm --filter ./artifacts/efootball-manager run dev",
    "dev:mock": "pnpm --filter ./artifacts/mockup-sandbox run dev",
    "dev:all": "pnpm -r --parallel --filter \"./artifacts/**\" --if-present run dev",
    "migrate": "node ./scripts/apply_migrations.js",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
